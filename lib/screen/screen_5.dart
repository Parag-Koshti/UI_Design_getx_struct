import 'package:flutter/material.dart';

class block extends StatelessWidget {
  const block({super.key});
  static String pageId = "/screen_5";
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("bye"),
      ),
    );
  }
}
