import 'package:flutter/material.dart';

class stock extends StatelessWidget {
  const stock({super.key});
  static String pageId = "/screen_4";
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("bye"),
      ),
    );
  }
}
