import 'package:flutter/material.dart';
import 'package:icons_plus/icons_plus.dart';

class wallet extends StatelessWidget {
  const wallet({super.key});
  static String pageId = "/screen_2";
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("hi"),
      ),
    );
  }
}
